import React from 'react';

const GetDesiredFlights = ({ desiredFlights }) => {
  console.log("desiredFlights in AnotherComponent", desiredFlights);
  
  return (
    <div>
      
    </div>
  );
};

export default GetDesiredFlights;
